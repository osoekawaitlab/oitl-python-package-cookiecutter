# {{cookiecutter.package_name}} Package Metadata Verification

This specification ensures that the `{{ cookiecutter.package_name }}` package is properly configured and follows industry standards for versioning.

## Ensure version follows Semantic Versioning
* Import the "{{ cookiecutter.package_name }}" package
* Verify that "__version__" follows the Semantic Versioning format
